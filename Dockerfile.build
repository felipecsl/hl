FROM rust:latest

WORKDIR /build

# Copy the project files
COPY . .

# Build the release binary
RUN cargo build --release --target x86_64-unknown-linux-gnu

# The binary will be in target/x86_64-unknown-linux-gnu/release/hl
